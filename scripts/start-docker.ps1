# Starts the full Dockerized template stack after you have prepared a Docker-ready env file.
# Copy .env.docker.example to .env before using this script for containerized startup.
docker compose up --build
